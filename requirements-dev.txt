# Everything in the app, plus the tooling to run the analysis notebook locally.
-r requirements.txt

# Notebook visualisation + execution
matplotlib>=3.8
seaborn>=0.13
jupyter
nbformat
nbconvert
ipykernel

# Only needed to regenerate the one-page PDF (scripts/build_pdf.py).
# After installing, also run: playwright install chromium
playwright
